import { makeStyles, alpha } from '@material-ui/core/styles';

export default makeStyles(() => ({
    background: {
        backgroundColor: '#112e51',
        width: '100%',
        maxWidth: '100%',
        height: '100%',
        minHeight: '100vh',
        padding: 0
    }
}));
